.PHONY: all serve build clean

all: serve

serve: ## Renders the documentation locally and serves it for live viewing.
	mkdocs serve

build: ## Builds the documentation into a static site.
	mkdocs build

clean: ## Removes the built site directory.
	rm -rf site/
