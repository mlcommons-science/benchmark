// This script initializes the DataTables plugin on the table.
// It waits for the document to be fully loaded before running.

$(document).ready(function() {
    // Select the table by its ID and initialize DataTables
    $('#example').DataTable();
});